---
import ThemeController from "./header/ThemeController.svelte";
import HueController from "@/components/header/HueController.svelte";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<header class:list={[className, "card flex justify-between px-4 py-3"]}>
  <HueController client:only="svelte" />
  <ThemeController client:only="svelte" />
</header>
