server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location ~ ^(.+)/$ {
    return 308 $1$is_args$args;
  }

  location / {
    try_files $uri $uri.html $uri/index.html =404;
  }

  location = /404.html {
    internal;
  }
}
